/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          '127.0.0.1',
         "books.google.com",
         "images-na.ssl-images-amazon.com"
        ],
    }
}

module.exports = nextConfig
